package com.android;
/**
 * a nested inner class is a class inside a class of which there are four types
 * A member inner class, defined at same level as instance variables and is NOT static
 * a local inner class that is defined within a method
 * a anonymous inner class which is a inner class that does not have a name
 * a static method that is defined a the same level as a static variable
 * @author noelf
 * Animal class for member inner class
 * Human class for Local inner classes
 * Building class for Anonymous inner classes
 * Doctor for static inner classes
 *
 */
public class Main {

	public static void main(String[] args) {
		//Examples.memberInner();
		//Examples.localInner();
		//Examples.anonymousInner();
		Examples.staticInner();
		

	}

}
